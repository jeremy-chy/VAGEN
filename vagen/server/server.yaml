defaults:
  - _self_

server:
  host: 0.0.0.0
  port: 22220
  debug: true

frozenlake:
  max_workers: 5
svg:
  max_workers: 10
  model_size: "small"
navigation:
  max_workers: 10
  devices: [0,1,2,3]

